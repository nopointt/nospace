<!-- ENTRY:2026-03-16:CHECKPOINT:136:nospace:content-router-design -->
## 2026-03-16 — checkpoint 136

**Decisions:**
- Система называется **Content Router**, не summarizer — цель lossless routing, не lossy compression
- Read/Bash/Grep tool results = only in Messages (подтверждено через /context до/после Read)
- tool_results от Read хранятся в Messages как tool_result items, не как "loaded documents"
- DROP стратегия для tool_results агрессивная: каждый Read = ~6.6k+ tokens, recoverable → первый кандидат на удаление
- assistant text blocks = уже сжатый вывод из прочитанных файлов → KEEP (важнее чем казалось)
- Нужен новый memory файл **why.md**: хранит rationale/trade-offs ("почему" за каждым решением)
- Текущая архитектура L0-L4 не имеет слоя WHY — это главный пробел

**Files changed:**
- нет

**Completed:**
- Анализ структуры JSONL (типы, поля, usage, content polymorphism)
- Подтверждено: tool results только в Messages, нет отдельного "loaded documents" layer
- Taxonomy восстанавливаемости: Read/Bash/Grep → DROP, WebFetch/WebSearch → compress, Write → KEEP fact
- Веса контента: user typed decision=1.0, assistant text=0.9, tool_call name=0.3, tool_result=0.05
- Концептуальный сдвиг: summarizer → Content Router

**In progress:**
- Дизайн Content Router (три открытых вопроса: загрузка why.md, timing роутера, кто пишет why.md)

**Opened:**
- Создать `memory/why.md` — структура и формат
- Ответить на 3 вопроса дизайна Router перед кодом
- Написать router hooks: mem-stop.ts, mem-session-start.ts, quota-guard.ts

**Notes:**
- Ключевой инсайт: thinking blocks = buffer для обработки файлов, их DROP безопасен
- why.md должен быть компактным если загружается в каждый SessionStart
- Content Router routing map: L1→architecture facts, L2→phase status, L3→task status, L4→checkpoint, auto-scratch→file changes, why.md→rationale, DROP→tool outputs/reads/noise
